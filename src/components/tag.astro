<div class="tag">
  <slot />
</div>

<style lang="scss">
  .tag {
    font-size: 0.7rem;
    color: var(--color-card-bg);
    background-color: var(--color-purple);
    border-radius: 10px;
    padding: 0.1em 0.7em;
    display: inline-block;
    margin: 0;
  }
</style>
